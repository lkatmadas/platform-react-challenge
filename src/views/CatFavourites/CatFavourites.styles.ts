import styled from '@emotion/styled'

const CatFavourites = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.5rem;
  padding-inline: 1.25rem;
`

const Styled = {
  CatFavourites,
}

export default Styled
