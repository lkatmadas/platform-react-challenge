import styled from '@emotion/styled'

const BreedStatsItem = styled.div`
  display: flex;
  justify-content: space-between;
  gap: 1.5rem;
`

const Styled = {
  BreedStatsItem,
}

export default Styled
